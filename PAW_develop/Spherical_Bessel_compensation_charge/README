Run it like: ./model_PAW.x PAW.in n_r n_theta n_phi l_max
Currently: n_r <= 14

For force checker: 
./compile.x $1 _FORCECHECK_
where $1 is FORT_UNDER_OFF for mac
			FORT_UNDER     for other platforms 
